# Optional: enables exact-address marker placement through Google Maps Geocoding API.
# Do not commit real keys. Set this in your shell, service manager, or local .env tooling.
GOOGLE_MAPS_API_KEY=

# Optional server upload controls.
GEOCODE_LIMIT=50
# Set to 1 to replace existing approximate/city-level coordinates during server uploads.
GEOCODE_REFRESH=0
